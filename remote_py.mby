{"config.json":"{\"mode\":\"code\",\"github\":null,\"board\":\"easy-kids-robot-kit\",\"level\":\"Beginner\"}","main.py":"from board import rgbled_board\nfrom board import motor\nimport bluepad32\n\nleft_motor = None\nleft = None\nright_motor = None\nforward = 0\nturnPower = None\nmaxPower = None\nminPower = None\n\n\nrgbled_board.set_brightness(30)\nrgbled_board.fill('#ff0000')\nrgbled_board.show()\nprint('Ready')\nmotor.set_frequency(5000)\nmaxPower = 60\nminPower = maxPower * -1\nturnPower = 30\n\nwhile True:\n  if bluepad32.is_connected():\n    if bluepad32.l1():\n        # slide left\n      motor.move(motor.SLIDE_LEFT, maxPower)\n    elif bluepad32.r1():\n        # slide right\n      motor.move(motor.SLIDE_RIGHT, maxPower)\n    else:\n\n        if (bluepad32.axisY()) > 20:\n            forward = minPower * bluepad32.axisY() / 512;\n        elif (bluepad32.axisY()) < -20:\n            forward = maxPower * bluepad32.axisY() / - 508;\n        else:\n            forward = 0\n        \n        #  end forward detect\n        if(-5 < forward and forward < 5): \n            # turn no run\n            if(bluepad32.axisRX() > 20):\n                #   turn right slow down right motor\n                left_motor = turnPower * (bluepad32.axisRX() / 512)        \n            elif bluepad32.axisRX() < (-20):\n                #   turn left slow down left motor\n                left_motor = 0\n                right_motor = turnPower * (bluepad32.axisRX() / -508)        \n            else:\n                left_motor = 0\n                right_motor = 0\n        elif (forward > 5):\n            # run and turn\n            \n            if(bluepad32.axisRX() > 20):\n                #   turn right slow down right motor\n                right_motor = forward - (turnPower * (bluepad32.axisRX() / 512))        \n                left_motor = forward\n            elif bluepad32.axisRX() < (-20):\n                #   turn left slow down left motor\n                left_motor = forward - (turnPower * (bluepad32.axisRX() / -508))\n                right_motor = forward\n            else:\n                right_motor = forward\n                left_motor = forward\n        elif (forward < -5):\n            if(bluepad32.axisRX() > 20):\n                #   turn right slow down right motor\n                right_motor = forward + (turnPower * (bluepad32.axisRX() / 512))        \n                left_motor = forward\n            elif bluepad32.axisRX() < (-20):\n                #   turn left slow down left motor\n                left_motor = forward + (turnPower * (bluepad32.axisRX() / -508))\n                right_motor = forward\n            else:\n                right_motor = forward\n                left_motor = forward\n        else:\n                right_motor = 0\n                left_motor = 0\n        # print((''.join([str(x) for x in ['LY ', bluepad32.axisY(), ' Left Motor ', left_motor, ' RX ', bluepad32.axisRX(), ' Right Motor ', right_motor]])))\n        print((''.join([str(x) for x in ['LY ', bluepad32.axisY(),' RX ', bluepad32.axisRX(),' forward ', forward, ' Left Motor ', left_motor, ' Right Motor ', right_motor ]])))\n        motor.wheel(left_motor, left_motor, right_motor, right_motor)\n"}